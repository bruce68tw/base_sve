//crud fun type, same to c#
export enum CrudEnum {
    authRow,    //pos 0, fun is auth row or not(0/1)
    create,
    read,
    update,
    delete,
    print,
    export,
    view,
    other,
    empty = 99
}